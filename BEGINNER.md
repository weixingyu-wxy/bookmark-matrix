# 📖 零基础安装教程 — Bookmark Matrix

> 不会编程？不会装扩展？没关系。这份教程每一步都有说明，跟着做 5 分钟装好。

---

## 📚 在开始之前，先搞清楚几个名词

| 名词 | 是什么意思 |
|------|----------|
| **Edge 扩展** | 装在 Edge 浏览器里的小程序，可以给你的浏览器加新功能 |
| **PWA (渐进式 Web 应用)** | 一种像普通 App 一样能装到桌面的网页 |
| **开发者模式** | Edge 的一个开关，允许你装"还没上架商店"的扩展 |
| **src 文件夹** | 扩展的源代码（程序文件）所在位置 |
| **解压** | 把 `.zip` 文件拆开变成普通文件夹 |

---

## 🚀 安装方式 — 推荐: 方式 A (Edge 扩展)

这种方式扩展就在 Edge 工具栏里，最方便。

### 步骤 1: 确认你有 Edge 浏览器

- 打开开始菜单 (左下角 Windows 图标)
- 搜 "Edge" 或 "Microsoft Edge"
- 如果搜不到 → 打开 https://www.microsoft.com/edge 下载安装

### 步骤 2: 下载扩展文件

你看到这份教程说明已经有了 `edge-bookmark-matrix-v1.2.0-src.zip` 这个文件。它长这样：

```
📁 下载文件夹
  📄 edge-bookmark-matrix-v1.2.0-src.zip     ← 就是这个
  📄 其他文件...
```

> 💡 **zip 文件是什么**: 像一个压缩的文件夹，Windows 自带解压。

### 步骤 3: 解压文件

**方法 A (Windows 11 / 10 推荐)**:
1. 在 `edge-bookmark-matrix-v1.2.0-src.zip` 上 **点右键**
2. 选 "**全部解压...**" (Extract All...)
3. 弹窗里选保存位置 (建议桌面，方便找)
4. 勾选 "完成后显示提取的文件"
5. 点 "**提取**"

**方法 B (没看到 "全部解压" 怎么办)**:
1. 双击 zip 文件打开
2. 里面有一个 `src` 文件夹
3. 把 `src` 文件夹 **拖到桌面** (或任何你找得到的位置)

### 步骤 4: 记住 src 文件夹的位置

假设你解压到桌面，那路径是：

```
C:\Users\你的用户名\Desktop\src\
```

或者你拖到 D 盘的某位置 — 都行，但要记住。

**如果不确定在哪**:
1. 打开文件资源管理器 (任务栏黄色文件夹图标)
2. 进入桌面
3. 应该看到 `src` 文件夹
4. 双击 `src` 进入 → 应该看到一堆文件 (`manifest.json`, `popup.html` 等)

### 步骤 5: 打开 Edge 扩展管理页

1. 打开 Edge 浏览器
2. 在顶部地址栏 (就是输网址的地方) 复制粘贴下面这行：

```
edge://extensions/
```

3. 按回车 (Enter)

会看到一个页面，标题是 "扩展"。长这样：

```
┌─────────────────────────────────────┐
│ 扩展                       ⚙ 设置  │
│                                     │
│ ☑ 开发人员模式                      │  ← 重要！要打开
│                                     │
│  [加载解压缩的扩展] [打包扩展] [更新] │
│                                     │
│  (这里目前是空的)                   │
└─────────────────────────────────────┘
```

### 步骤 6: 打开 "开发人员模式"

找到页面 **左下角或右上角** 的 "**开发人员模式**" 开关。

- 它是个 **可点击的小开关** (toggle)
- 点击它，让它变成 **蓝色/开启** 状态

> 💡 如果找不到，可能需要先点页面顶部的 "**开发者**" 标签页 (Edge 某些版本)。

### 步骤 7: 加载扩展 (两种方法二选一)

#### 方法 1: 拖拽 (最简单)

1. 打开文件资源管理器，进入 `src` 文件夹
2. **按住 `src` 文件夹不放，拖到** Edge 的 `edge://extensions/` 页面里
3. 看到 "扩展已加载" 提示 → 成功！

#### 方法 2: 点击按钮

1. 在 `edge://extensions/` 页面点 "**加载解压缩的扩展**" 按钮
2. 弹出文件选择窗口
3. 进入 `src` 文件夹所在位置 (比如桌面)
4. **点进 `src` 文件夹** (双击进入)
5. 点 "**选择文件夹**" 按钮

> ⚠️ **重要**: 一定要选 `src` 文件夹 **里面**，不是选 `src` 这个文件夹本身！

### 步骤 8: 固定到工具栏

1. 看到页面里出现 "Bookmark Matrix — 书签矩阵" 卡片 → 装好了
2. 关闭 `edge://extensions/` 页面
3. 看 Edge **右上角** — 有一个 **🧩 拼图图标**
4. **点一下拼图图标**
5. 弹出 "扩展" 列表
6. 找到 "Bookmark Matrix"
7. 旁边有个 **眼睛图标 👁** (或者"显示在工具栏上")
8. **点一下** → 图标变成蓝色

### 步骤 9: 完成 — 试试看

1. Edge 工具栏现在应该有一个 **▦ 蓝色方块图标** (或者别的彩色方块)
2. **点它** → 弹窗打开
3. 看到分类矩阵了吗？✨ 装好了！

---

## 🆘 遇到问题了？对照检查

### ❌ 问题 1: 找不到 "开发人员模式" 开关

**原因**: Edge 版本太新或太旧。

**解决**:
1. 确认 Edge 已更新: 菜单 ⋯ → "帮助和反馈" → "关于 Microsoft Edge" → 自动更新
2. 如果还找不到，搜索 "Edge 加载解压缩的扩展" 看具体 Edge 版本的操作

### ❌ 问题 2: 拖拽没反应 / "无法识别的文件类型"

**原因**: 没选到 `src` 文件夹，或者选到了 zip 文件。

**解决**:
1. 确认是 **解压后的 `src` 文件夹**，不是 zip 压缩包
2. `src` 文件夹里应该能看到 `manifest.json` 等文件
3. 如果文件夹是空的 → 重新解压

### ❌ 问题 3: 装好后弹窗是空白

**原因**: 权限问题。

**解决**:
1. 重新打开 `edge://extensions/`
2. 找到 "Bookmark Matrix" 卡片
3. 点 "**详细信息**" (或 "...")
4. 找到 "**网站访问权限**"
5. 改成 "**所有站点**"
6. 关掉页面，重新点工具栏图标

### ❌ 问题 4: 弹窗打开但看不到书签

**原因**: 你的 Edge 收藏夹是空的。

**解决**:
1. 在 Edge 打开几个常用网站 (baidu.com, github.com 等)
2. 按 `Ctrl + D` → 点 "完成" → 收藏
3. 收藏 5-10 个后，再点工具栏的 ▦ 图标

### ❌ 问题 5: 加载时弹错 "无法加载清单"

**原因**: 选错了文件夹层级。

**解决**:
- 不要选 zip 文件
- 不要选包含 src 的 **父文件夹** (比如解压出来的 `edge-bookmark-matrix-v1.2.0` 文件夹)
- 要选 `src` 文件夹 **本身** — 里面要有 `manifest.json`

### ❌ 问题 5.5: "无法加载扩展 - Default locale was specified, but _locales subtree is missing"

**原因**: 这个错误只出现在 **v1.2.0 早期版本**。已修复。
新版 zip 里 manifest.json 已删除 `default_locale` 字段。

**解决**:
1. 重新下载 `edge-bookmark-matrix-v1.2.0-full.zip` 或 `-src.zip`
2. 解压新版，**删掉旧的 src 文件夹**再用新的
3. `edge://extensions/` 点 ↻ 重新加载
4. 不再报错

> 💡 如果还报错，去 `edge://extensions/` 找 Bookmark Matrix → 删除 → 重新加载新版 src。

### ❌ 问题 6: 工具栏看不到图标

**原因**: 没固定到工具栏。

**解决**:
1. Edge 右上角找 🧩 拼图图标
2. 点它 → 弹出扩展列表
3. 找 Bookmark Matrix → 点 👁 眼睛图标

### ❌ 问题 7: 分类不准确 / 书签被分错类

**完全正常！** 自动分类不保证 100% 准确。

**解决**:
1. 弹窗里 **右键** 任意书签
2. 选 "移动到分类" → 选正确分类
3. Edge 会记住你的选择，下次不会错

### ❌ 问题 8: 装好后完全没反应

**解决**:
1. `edge://extensions/` 找 Bookmark Matrix
2. 确认开关是 **蓝色/启用** 状态
3. 点 "**重新加载**" ↻ 按钮
4. 再试

### ❌ 问题 9: "您的浏览器不受支持" / "需要 Chrome 88+"

**原因**: Edge 太旧。

**解决**:
1. Edge 菜单 ⋯ → "帮助和反馈" → "关于 Microsoft Edge"
2. 自动检查更新
3. 重启 Edge

### ❌ 问题 10: 占用内存高 / Edge 变慢

**可能原因**: 健康检查同时发起太多网络请求。

**解决**:
1. 弹窗里 **不要** 点 🩺 "全部检测" — 先小批量
2. 或者完全不用健康检查功能

### ❌ 问题 11: 怎么彻底卸载

**步骤**:
1. `edge://extensions/`
2. 找 "Bookmark Matrix" 卡片
3. 点 "**删除**"
4. 确认 → 完成

### ❌ 问题 12: 怎么升级版本

**步骤**:
1. 下载新版 zip
2. 解压
3. **覆盖** 老的 `src` 文件夹 (或者改个名 `src-v2`)
4. `edge://extensions/` → 找 Bookmark Matrix
5. 点 "**重新加载**" ↻
6. 你的数据 (overrides/标签) 自动保留

### ❌ 问题 13: 标签/备注/收藏丢了

**原因**: 卸载扩展会清空本地数据，原始 Edge 收藏夹不受影响。

**解决**:
- 重新加标签 (有 JSON 备份的话从备份恢复: ↕ → 导入)

### ❌ 问题 14: 想换电脑 / 多设备同步

**步骤**:
1. 设备 A: 弹窗点 ↕ → "**导出备份**" → 保存 `.json` 文件
2. 把 `.json` 文件传到设备 B (U盘/网盘/邮件)
3. 设备 B: 装好扩展 → 弹窗点 ↕ → "**选择文件并合并导入**"
4. 数据就同步过来了

### ❌ 问题 15: 真的搞不定怎么办

**选项 A**: 用 PWA 模式 (下面教程) — 不需要装扩展
**选项 B**: 找身边会电脑的朋友帮忙
**选项 C**: 提 Issue: https://github.com/weixingyu-wxy/xiuxian-world/issues (附上截图)

---

## 🅱️ 安装方式 B: PWA 桌面版 (可选)

这种是把 Bookmark Matrix 装成桌面图标，**不需要装扩展**。

**适合**:
- 不喜欢装扩展
- 想跨浏览器用
- 想有桌面图标

### 步骤 1: 准备文件

下载 `edge-bookmark-matrix-v1.2.0-full.zip`，解压。
解压后找到 `standalone` 文件夹 (跟 `src` 平级)。

### 步骤 2: 部署到 Netlify (免费)

1. 打开 https://app.netlify.com/drop (用 Edge 浏览器)
2. 看到一个大大的虚线框 "**Drag and drop your site output folder here**"
3. 打开文件资源管理器
4. 找到 `standalone` 文件夹
5. **把整个 standalone 文件夹拖进** 那个虚线框
6. 等待 3-10 秒 → 出现一个网址 (类似 `https://xxx.netlify.app`)
7. **复制这个网址** (Ctrl+C)

### 步骤 3: 打开 Bookmark Matrix

1. 在 Edge 地址栏粘贴刚才复制的网址
2. 按回车
3. 看到 Bookmark Matrix 界面 → 成功

### 步骤 4 (可选): 安装为桌面 App

1. 打开后等几秒
2. Edge 地址栏右侧会出现一个 **"..." 或安装图标**
3. 点 → 选 "**安装 Bookmark Matrix**"
4. 桌面会多一个图标
5. 双击图标 → 像原生 App 一样启动

### 步骤 5 (重要): 导入你的 Edge 书签

PWA 默认有 7 条演示数据。要换成你自己的：

1. 打开 Edge 收藏夹管理: 地址栏输入 `edge://bookmarks/` 回车
2. 右上角 ⋯ → "**导出书签**" → 保存 `.html` 文件
3. 回到 Bookmark Matrix 窗口
4. 顶部 **↕** → "导入 Edge 书签 HTML" → "**选择 HTML**"
5. 选刚才保存的 `.html` 文件
6. 你的书签全部导入 → 自动分类

---

## 🆘 PWA 模式问题

### ❌ "Drag and drop" 没反应
- 拖的是 `standalone` 文件夹本身 (里面包含 index.html)，不是 zip
- 浏览器可能要等待几秒

### ❌ 网址打不开 / 404
- Netlify 部署可能失败 — 看页面提示
- 重新拖一次

### ❌ PWA 不能离线
- 必须用 HTTPS 网址 (Netlify 默认是)
- `file://` 协议不能注册 service worker

### ❌ "安装" 按钮不出现
- 必须用 Edge / Chrome 浏览器
- 必须 HTTPS 网址
- 某些 Edge 版本不显示 — 正常，PWA 不强制要求

---

## 💡 上手后建议做的几件事

按顺序来，10 分钟搞定：

1. ✅ **导入你的 Edge 书签** (如果有 PWA) — 上面有步骤
2. ✅ **右键几个分错的书签** — 让 Edge 学会你的习惯
3. ✅ **加几个标签** — 右键 → 编辑标签 → "work, 重要" 这种
4. ✅ **⭐ 收藏你最常用的** — 右键 → 切换收藏
5. ✅ **进全屏模式** — 弹窗右上角 ⛶ 按钮，拖动左侧分类自定义顺序
6. ✅ **导出第一次备份** — 顶部 ↕ → 导出备份，保存在网盘
7. ✅ **(可选) 检测死链** — 顶部 🩺 按钮，等几秒看结果
8. ✅ **(可选) 开定期提醒** — 顶部 ⏰ 按钮 → 启用 → 保存

---

## 🎉 装好之后

你看到分类矩阵了吗？搜索框输入关键词试试 — 应该立刻过滤。

**记住几个快捷键**:
- `Ctrl + F` (弹窗) 或 `Ctrl + K` (全屏) → 快速聚焦搜索
- `Esc` → 关闭弹窗 / 清空搜索
- 左键 = 打开 / `Ctrl + 点击` = 后台打开 / 中键 = 后台 / 右键 = 菜单

---

## 📞 还有问题？

- **README.md** — 功能介绍
- **INSTALL.md** — 进阶用户的快速参考
- **GitHub**: https://github.com/weixingyu-wxy/xiuxian-world/issues (提 Issue)

**作者**: Xingyu Wei
**AI 协助**: Mavis (MiniMax Code)

---

🎉 祝用得开心！再多的书签也不怕找不到了。
